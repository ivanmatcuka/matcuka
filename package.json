{
  "name": "matcuka-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "be:dev": "docker compose up --build strapi-dev",
    "be:prod": "docker compose up --build strapi"
  }
}
